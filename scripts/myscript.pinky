-----------------------------------------------------------
-- Function to print the factorial of a number
-----------------------------------------------------------
func factorial(n)
  mul := 1
  for i := 1, n do
    mul := mul * i
  end
  ret mul
end

println factorial(6)
